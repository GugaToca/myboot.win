# Myboot.win — configuracao do Netlify
# Site estatico: nao ha build, publica a pasta como esta.

[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
